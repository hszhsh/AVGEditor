import { Emitter } from "@/utils/event-kit";

let emitter = new Emitter();

export namespace EDITOR_EVENT {
    // export let REFRESH_CANVAS = emitter.createEvent<(data: Node) => void>("REFRESH_CANVAS");
    // export let SET_MOUSE_CURSOR = emitter.createEvent<(cursor: string) => void>("SET_MOUSE_CURSOR");
}
