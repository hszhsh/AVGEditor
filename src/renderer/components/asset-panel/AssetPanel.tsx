import * as React from "react";
import { FileBrowserView } from "../file-browser/FileBrowserView";

export default class AssetPanelComponent extends React.Component {
    render() {
        return (
            <FileBrowserView />
        );
    }
}