import * as React from "react";
import { Key } from "@/renderer/common/utils";
import { Divider } from "antd";

export const PropertyDivider = (props: { selectedSceneNodeKey: Key }) => {
    return <Divider style={{ margin: "10px 0px" }} />
}